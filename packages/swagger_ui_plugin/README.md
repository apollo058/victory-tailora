# Swagger UI plugin

Planned frontend integration that adds an Inspector tab to the existing Swagger UI through its plugin and custom layout APIs.

The plugin should consume the Inspector API and should not fork or modify Swagger UI core.

This package is the future home of the Inspector UI. The project does not need a separate top-level React application for the product runtime.
