# FastAPI example

Run the example from the repository root:

```bash
python -m uvicorn examples.fastapi.app:app --reload
```

The `GET /health` endpoint returns `{"status":"ok"}`. Inspector integration will be added in a later step.

